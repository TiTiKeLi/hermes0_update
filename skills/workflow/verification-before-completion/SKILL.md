---
name: verification-before-completion
description: 在宣称工作完成、已修复或测试通过之前使用——必须运行验证命令并确认输出后才能声称成功；始终用证据支撑断言
category: workflow
conditions:
  requires_toolsets:
    - terminal
    - file
platforms: [linux]
triggers:
  keywords:
      - 完成
      - 修复
      - 测试通过
      - 验证
      - 确认
      - 上传
      - 部署
      - 搞定
      - 完成了
      - 成功
  events:
      - task_completion
---

# 完成前验证

## 概述

在没有验证的情况下宣称工作完成，这不是高效，而是不诚实。

**核心原则：** 始终用证据支撑结论。

## 铁律

```
没有新鲜的验证证据，不许宣称完成
```

如果你在回复中没有运行验证命令，就不能声称测试通过、Bug已修、构建成功。

---

## 1. 边界条件

### 入口条件（必须全部满足）
- [ ] 正在执行有明确可验证产出的任务（测试、修复、构建、部署、上传、**技能创建/修改**）
- [ ] 有可执行的验证命令（测试命令、构建命令、linter、curl 健康检查、**skill_view + delegate_task**）
- [ ] 验证命令产生了可读的输出（exit code，stdout，或子智能体返回结果）

### 跳过条件（一条即跳过）
- [ ] 纯文字/创意类工作（无代码产出）
- [ ] 用户明确说"不用验证，直接发"
- [ ] 验证环境完全不可用（网络不通、依赖缺失）
- [ ] 概念讨论或设计阶段

### 中止条件（执行中，一条即停）
- [ ] 验证命令超时（>120s）且无法简化
- [ ] 验证环境损坏（如编译器缺失、测试框架未安装）
- [ ] 用户插入新指令切换话题

---

## 2. 决策矩阵

| 场景 | 行动 | 工具链 | 产出 |
|------|------|--------|------|
| 技能创建/修改 | `skill_view` 加载确认 → 用 `delegate_task` 分派独立子智能体验证每个更新 | `skill_view`, `delegate_task` | 技能可加载确认 + 子智能体独立验证结果 |
| 测试通过/失败 | 运行完整测试命令→读输出→统计失败数 | `terminal` | 测试结果+失败列表 |
| Bug 修复 | 重新运行触发 Bug 的原始命令/测试→确认通过 | `terminal` | 修复验证输出 |
| 构建/编译 | 重新运行完整构建命令→确认 exit 0 | `terminal` | 构建日志 |
| 文件写入 | `read_file` 回读→确认内容完整 | `read_file` | 文件内容确认 |
| 部署/上传 | curl/HTTP 健康检查+响应码验证 | `terminal`(curl) | 部署状态 |
| 网络请求完成 | 读实际 HTTP 响应/状态码，不靠假设 | `terminal`(curl) | 响应体/状态码 |

---

## 3. 原子步骤

### Step 1: 确定验证手段

**输入**：当前任务的产出物
**操作**：
1. 问自己：什么命令能证明这个结论？
   - 测试通过 → `pytest ...` 或 `npm test`
   - Bug 已修 → 重新运行原始失败场景
   - 文件已写 → `read_file` 回读
   - 网络请求完成 → curl 带实际输出
   - 技能创建/修改 → `skill_view` 加载 + `delegate_task` 分派独立子智能体验证（见 `references/skill-creation-verification-template.md`）
2. 选择最直接的验证方式（不是最快，是**最可靠**）

### Step 2: 执行完整命令（重新运行，不要用缓存）

**输入**：验证命令
**操作**：
1. 运行完整命令——不要用之前的输出，**重新跑**
2. 对于测试：完整执行（不仅仅是 changed files）
3. 对于修复：用和发现时完全相同的触发条件

### Step 3: 阅读完整输出

**输入**：命令输出
**操作**：
1. 查看 exit code
2. 统计失败数（测试 failure/error）
3. 读实际响应体（curl）、输出内容
4. 确认结果**明确支持**你的结论

### Step 4: 用证据陈述结论

**输入**：验证结果
**产出**：带证据的结论
**操作**：
- 如果通过：`✅ 输出：<实际输出摘要>，exit 0`
- 如果失败：`❌ 输出：<实际输出>，exit N，失败详情：...`
- 绝不使用：应该、大概、似乎、看起来

**验证**：
```python
# 检查结论是否有证据支撑
assert "exit" in conclusion or "输出" in conclusion or "测试" in conclusion
```

---

## 4. 工具/Skill 联动表

| 步骤 | 调用的工具 | 读取的文件 | 写入的文件 | 依赖的 skill |
|------|-----------|-----------|-----------|-------------|
| Step 1 | — | 当前任务上下文 | — | — |
| Step 2 | `terminal` | 测试/构建相关文件 | — | — |
| Step 3 | `read_file`(回读) 或 阅读 terminal 输出 | 命令输出 | — | — |
| Step 4 | — | Step 3 输出 | 结论 | `deep-need-analysis`(阻塞判断) |

---

## 5. 反馈回路

### 常见失败模式

| 结论 | 需要 | 不够格 |
|------|------|--------|
| 测试通过 | 测试命令输出：0 failures | 之前的运行结果、"应该会通过" |
| Bug 已修复 | 测试原始症状：通过 | 代码改了，假设已修复 |
| 构建成功 | 构建命令：exit 0 | linter 通过、日志看起来没问题 |
| 文件已写 | `read_file` 回读确认 | 写了但没读过 |
| 部署成功 | curl 健康检查：200 | 部署命令 exit 0 但服务未就绪 |

### 红线——立即停止检查

- 在结论中使用"应该"、"大概"、"似乎"
- 验证前就表达满意（"太好了！"、"完美！"、"搞定！"）
- 用之前的运行结果代替重新验证
- 只读了输出开头几行就下结论

### 迭代上下文

每次违反此技能的行为应当记录到 `research/verification-violations.md`：
```json
{
  "timestamp": "...",
  "claim": "...",
  "missing_evidence": "...",
  "corrected": true/false
}
```
