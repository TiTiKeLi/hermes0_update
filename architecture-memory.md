# Hermes 四层记忆架构 v2（完整定稿）

> 本文件是 Hermes 记忆系统的完整架构文档。
> 所有容量阈值、触发条件、数据流、评估机制均已确定。

---

## 架构总览

```
L1 原始层（Raw Layer）
═══════════════════════════════════════
  存储: sessions/ + incoming/ + state.db
  策略: 永久保留, 2GB 总上限
  超上限: 通知用户转移, 不自动删除
  访问: L3 只读

L3 温缓存层（Warm Cache）
═══════════════════════════════════════
  存储: memories/cache/（文件系统）
  定位: 存储层 + 评估层
  内容: L4 快照(10个) + 压缩历史 + 评估指标
  用途: L4 崩溃恢复, 压缩中转, 效果评估, 自动优化

L2 事实层（Fact Layer）
═══════════════════════════════════════
  存储: memory_store.db (SQLite) — facts 表
  最新访问版本上限: 3000/5000 条
  历史版本总上限: 1GB
  定位: 全量事实存储 + 成功信号写入点

L4 热缓存层（Hot Cache）
═══════════════════════════════════════
  存储: MEMORY.md
  容量: ≤2000 chars（原始 2K 标准）
  格式: IDENTITY + [p:NN] 优先级标记 FACTS
  定位: Agent 上下文缓存, 每次会话加载
```

---

## L1: 原始层

### 存储构成

| 数据源 | 位置 | 当前大小 |
|--------|------|---------|
| 会话记录 | sessions/session_*.json | 164 文件 |
| 状态数据 | state.db | 77MB |
| HTTP 转储 | incoming/request_dump_*.json | 若干 |
| JSONL 日志 | incoming/*.jsonl | 若干 |

### 策略

- **只追加, 不修改, 永久保留**
- 永久保留 = 在 2GB 总上限内永久保留
- **超过 2GB → 通知用户转移这部分会话信息到其他地方保存, 不自动删除**
- 当前已使用约 500MB
- 缓存文档 (memories/cache/) vs 大数据文档 (incoming/ + sessions/) 区分

---

## L2: 事实层

### 数据库结构

```sql
facts 表核心字段:
  fact_id         INTEGER PRIMARY KEY
  content         TEXT NOT NULL
  entity          TEXT NOT NULL
  tags            TEXT
  trust_score     REAL DEFAULT 0.5
  retrieval_count INTEGER DEFAULT 0
  last_accessed   INTEGER
  archived        INTEGER DEFAULT 0
  priority        INTEGER DEFAULT 50
  composite_score REAL
  version         INTEGER DEFAULT 1     -- 版本号, 每次压缩递增
```

### 版本机制（新增）

容量上限是**最新访问版本**的上限, 不是历史记录的总上限。

```
最新访问版本上限: 3000/5000 条
  └── 活跃事实 ≤3000 → 正常
  └── 3000 < 活跃 ≤5000 → 触发 L3 普通压缩
  └── 活跃 >5000 → 触发 L3 紧急压缩

历史版本总上限: 1GB
  └── 所有历史版本（archived=1 的旧版本事实）合计 ≤1GB
  └── 超过 1GB → 通知用户转移
```

### 转移冗余策略

```
L2 历史版本 ≥ 1GB → 通知用户转移
  │
  ├── 在 0.5GB 冗余内（1GB ~ 1.5GB）
  │     └── 不影响数据转移, 正常操作
  │
  └── 超过 1.5GB → 强制压缩
        ├── 保留: 权重高的事实
        ├── 遗弃: 权重低的事实（composite_score 低的）
        └── 目标: 内容至少降到 60%
```

### 成功信号

**任务完成 → 写入 L2 fact_store = 成功信号。** L4 写入是缓存刷新。

```
任务完成
  ├── L2 fact_store 写入（强制, ACID）← 成功信号
  ├── L3 缓存写入（可选）
  ├── L4 CURRENT 写入（尽力, ≤200 chars）
  └── 任务标记完成（L2 写入后即成功）
```

---

## L3: 温缓存层

### 定位

```
之前（错误的）: L3 是纯管道, 不存储。过程完数据丢。
现在（正确的）: L3 是存储层 + 评估层。
                L2→L3 压缩并存储 → L3→L4 刷新。
                L4 崩溃时从 L3 快照恢复, 不从 L2 重建。
```

### 存储结构

```
memories/cache/
├── l4-snapshots/              ← L4 快照（保留最后 10 个）
├── compression-history/       ← 每次压缩完整报告
├── evaluation/                ← 记忆效果评估
│   ├── metrics.json
│   └── adjustments.json
└── bigdata/                   ← 大数据引用索引
```

### L4 崩溃恢复

```
MEMORY.md 损坏或为空
  ├── 有 L3 快照 → 加载最近正常快照 ✅
  └── 无快照 → 从 L2 top-20 临时构建 → degraded
```

### 记忆效果评估

```
评估指标:
  1. 事实利用率: L4 中事实被 Agent 引用的次数
     连续 3 周期零利用率 → 归档
  2. 压缩有效率: 压缩比 + 信息损失
  3. 死循环规避率: 同一问题是否再次出现

自动优化:
  连续 3 次评估差 → 调整 Jaccard/composite_score/压缩间隔
```

---

## L4: 热缓存层

| 属性 | 值 |
|------|-----|
| 文件 | MEMORY.md |
| 目标 | **≤2000 chars**（原始 2K 标准） |
| 当前 | 1649 chars (82.5%), 接近 75% 触发线 |
| 格式 | IDENTITY + `[p:NN]` priority FACTS（降序） |
| 原则 | 不加三段式, 原生 IDENTITY+FACTS 最高效 |

### 写入保障（Else 完整处理）

```
任务完成 → 写 L4 CURRENT (~200 chars)
  ├── L4 ≤ 1800 → 直接写 ✅
  └── L4 > 1800 → 紧急降到 70% (1400)
                    → 写 CURRENT (1400+200=1600<2000 ✅)
                    → degraded, 下次 L3 优先刷新
```

---

## 四阶段压缩管线

### Phase 0: 评估
检查上次评估结果, 调整本次压缩参数。

### Phase 1: 去重合并
按 entity 分组 → Jaccard >0.7 合并 → 冲突检测。
同 entity 差异大 (Jaccard<0.7) → 各自保留, 各自评分。

### Phase 2: 优先级排序
composite_score = trust×0.5 + retrieval_rate×0.3 + recency×0.2
按 score 降序, priority = round(score×100)

### Phase 3: 相关性检查
锚点: L4 CURRENT + 24h 滑动窗口 entity 列表
匹配 → L4 候选; 不匹配 → 留在 L2
跨 24h continuity: 同一个 entity 昨天写了今天又出现 → 仍算相关

### Phase 3.5: 存储到 L3
备份当前 L4 + 压缩报告 → 保留 10 个快照

### Phase 4: L4 原子写入
write→fsync→rename, 超 2000 chars 从尾部裁剪

---

## 触发条件总表

| 条件 | 阈值/间隔 | 触发组件 |
|------|----------|---------|
| 定时(低活跃) | <5条/h, **每 3h** | L3-B 压缩 |
| 定时(高活跃) | >10条/h, **每 1h** | L3-B 压缩 |
| 容量触发 | L4 > 75% (>1500) | L3-B 紧急压缩 |
| 写回阻塞 | L4 CURRENT 写不下 | L3-B 紧急挤出 |
| 死循环检测 | **每 6h** | L3-A |
| 模式分析 | **每 12h** | L3-C |
| L2 软超限 | ≥3000 活跃 | L3-B 普通压缩 |
| L2 硬超限 | ≥5000 活跃 | L3-B 紧急压缩 |
| L2 历史超限 | ≥1GB | 通知用户转移 |
| L2 冗余耗尽 | ≥1.5GB | 强制压缩到 60% |
| L1 超限 | ≥2GB | 通知用户转移 |
| 频次切换 | 稳定 **3 轮**后 | 切换间隔 |
| 自动优化 | 连续 3 次评估差 | 调 Jaccard/权重 |

---

## 容量上限总表

| 层级 | 上限类型 | 上限值 | 超出后行为 |
|------|---------|--------|-----------|
| L1 | 总大小 | **2GB** | **通知用户转移, 不自动删除** |
| L2 | 最新访问版本 | **3000/5000 条** | 压缩/紧急压缩 |
| L2 | 历史版本总大小 | **1GB** | **通知用户转移** |
| L2 | 转移冗余 | **0.5GB (1GB~1.5GB)** | 不影响转移 |
| L2 | 冗余耗尽 | **>1.5GB** | **强制压缩到 ≤60%**（保留高权重, 遗弃低权重） |
| L3 | 快照保留 | **10 个** | 删除最旧 |
| L3 | 评估保留 | **30 天** | 删除旧记录 |
| L4 | MEMORY.md | **2000 chars** | 紧急降到 1400 |
| L4 | USER.md | **1375 chars** | 由现有压缩 |
| 归档 | 单文件 | **500KB** | 滚动 .v2.md |

---

## 关键阈值速查

| 阈值 | 值 | 说明 |
|------|-----|------|
| MEMORY.md 目标 | 2000 chars | 原始 2K 标准 |
| L2 活跃上限 | 3000 条 | 触发普通压缩 |
| L2 活跃硬上限 | 5000 条 | 触发紧急压缩 |
| L2 历史上限 | 1GB | 通知转移 |
| L2 转移冗余 | 0.5GB | 不影响操作 |
| L2 强制压缩 | >1.5GB 降到 ≤60% | 保留高权重, 遗弃低权重 |
| L1 上限 | 2GB | 通知转移 |
| Jaccard 合并 | >0.7 | Phase 1 |
| 死循环 M | 3 次/48h | L3-A |
| 频次切换 | 5 条/h | 切换间隔 |
| 稳定期 | 3 轮 | 防震荡 |
| 24h 窗口 | 滑动 | Phase 3 相关性 |
| 紧急挤出 | 降到 70% (1400) | L4 写保障 |
| 归档单文件 | 500KB | 滚动版本 |
| L3 快照 | 10 个 | 保留数 |

---


## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v2 | 2026-07-26 | 四层架构设计, L3 温缓存+评估, L4 [p:NN], 2000 chars |
| v2.1 | 2026-07-26 | L1超2GB通知转移, L2版本化(最新版3000/5000+历史版1GB), 转移冗余0.5GB |
| v2.2 | 2026-07-26 | 事件驱动替代轮询, hooks #45 event + #46 reliability cron, #44旧轮询删除 |

